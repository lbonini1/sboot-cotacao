spring:
  datasource:
    url: jdbc:h2:mem:dbcotacao
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
        enabled: true

camel:
  springboot:
    name: Camel

app:
  properties:
    api:
      cotacao: "https://economia.awesomeapi.com.br/last/USD-BRL"